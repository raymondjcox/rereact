{
  "name": "rereact",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "esbuild src/app.tsx --bundle --watch --inject:./snab-shim.js --outfile=dist/out.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.14.38",
    "typescript": "^4.6.4"
  },
  "dependencies": {
    "snabbdom": "^3.5.0"
  }
}
